services:
  app:
    image: localhost/lova-to-do_app:latest
    build: .
    restart: always
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "file:/data/prisma/dev.db"
    volumes:
      - lova-todo-data:/data

volumes:
  lova-todo-data:
    external: false
